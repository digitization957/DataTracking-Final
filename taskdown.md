# Task Log

- Built dummy Login page (Token + Role) that generates a base64 JWT-like string and redirects to Dashboard.
- Built Dashboard page that decodes JWT client-side, fetches user info from dummy DB (App_Data/users.json) via WebMethod, shows role-based UI.
- Built Upload page with 4 cascading category dropdowns, smart subject autocomplete, remark, multi-file upload (up to 8: pdf/image/msg/excel/word/ppt), and tag input with autocomplete + related-tag suggestions.
- Added UploadHandler.ashx to validate & save files (extension whitelist, size limit, GUID filenames) and persist record/subject/tag metadata to JSON files in App_Data.
- Added Helpers/JsonStore.cs for reading/writing JSON data files.
- Hardened Web.config (request size limits) for file upload support; all data/UI logic uses plain HTML/JS/AJAX + WebMethods only, no MVC controllers.
- Added Database/schema.sql with the real table design (Users, Categories, Subjects, Tags, Records, RecordFiles, RecordTags, SubjectTags).
- Published two Claude artifacts: a schema/flow reference sheet, and a fully clickable Login → Dashboard → Upload demo of the whole app.
- Simplified to a single role: removed the Role dropdown from Login and all role branching from Dashboard/Upload.
- Added Repository.aspx: filterable file/log browser (department/category/subject/tags/date), with inline PDF/image preview, .msg served for direct Outlook open, and download for Office files.
- Added FileHandler.ashx to stream stored files securely (record/file validated against records.json, GUID-based paths, no path traversal).
- Rebuilt the "DataTracking Console" Claude artifact into a full clickable prototype of Login → Dashboard → Upload → Repository, including live file preview for uploaded images/PDFs.
- Redesigned the whole app UI/UX (Hallmark): new Cobalt design system (design.md + Content/tokens.css + Content/app.css) replacing Bootstrap defaults; Login is now an auth card, Dashboard an instrument panel with real record/tag/department counts (new GetStats WebMethod), Upload a two-pane workbench, Repository a filter-rail data browser. All existing element IDs/AJAX contracts preserved untouched.
- Wired Dashboard's name lookup to the org's existing Access DB (login_tokenpass: Token, Name) via a new parameterized Helpers/LoginDb.cs (OleDb) and a dummy "LoginDb" connection string in Web.config — falls back to App_Data/users.json if the DB isn't reachable yet. Added Database/schema.md documenting the full app schema (external login_tokenpass + the app's own SQL Server tables).
- Switched target DB from Access/SQL Server to Azure Database for MySQL (two separate databases: LoginDb for login_tokenpass, AppDb for the app's own tables). Vendored MySqlConnector 2.3.7 (packages.config + csproj reference), rewrote Helpers/LoginDb.cs to use it, added dummy AppDb/LoginDb MySQL connection strings in Web.config, converted Database/schema.sql to MySQL DDL, and updated schema.md.
- Repository file previews: PDF/image now open inline in a new browser tab (was an in-page modal), .msg keeps opening via Outlook mime type, everything else force-downloads; removed the now-unused preview modal markup/JS.
- Added Master.aspx: a "Master ▾" navbar dropdown (added to every page) opening a 4-column cascade manager (Department → Category → Sub-Category → Type) where you click through columns and add/delete options inline; Upload/Repository already read the same categories.json, so new options show up there immediately. Delete cascades to all child options. Updated Database/schema.md and schema.sql (Categories self-FK now ON DELETE CASCADE) to document it.
- Full real-DB migration: removed all App_Data/*.json dummy files and Helpers/JsonStore.cs. Added Helpers/AppDb.cs and rewired Master/Repository/Upload/UploadHandler/FileHandler/Dashboard to read and write Categories/Subjects/Tags/Records/RecordFiles/RecordTags/SubjectTags in AppDb (Azure MySQL, dummy connection string) via MySqlConnector. Dropped the app-owned Users table from schema.sql/schema.md; Records now stores Token directly.
- Identity flow changed: app now takes Token+Role only from the incoming JWT (`?jwt=...`) or a plain query string (`?token=...&role=...`) via new Scripts/auth.js (DTAuth.resolve()), cached in sessionStorage for in-app navigation. Role is shown as-is (no lookup/storage); Name is still resolved server-side from Token via LoginDb's login_tokenpass table, now pointed at the org's "access" database. Added a role/token popover under the user chip on every page (was a plain label), with Logout. Login.aspx is now a dev-only manual entry point (token+role fields), no longer fabricates a fake JWT.
- UI pass: themed chevron on all `<select>` dropdowns (was mismatched native arrow), avatar-initials user icon + popover (Name/Role/Token/Logout) shared via Scripts/auth.js helpers, Dashboard now shows live Recent activity + By-department breakdown from AppDb.
- Fixed crash on real DB connect: `TypeInitializationException` on `MySqlConnector.Logging.MySqlConnectorLoggingConfiguration` — MySqlConnector's own dependencies (Microsoft.Extensions.Logging.Abstractions, System.Diagnostics.DiagnosticSource, System.Threading.Tasks.Extensions) were never vendored/referenced. Added all three to packages/, csproj, packages.config, and Web.config binding redirects. Also filled in the real Azure MySQL host/Uid/Pwd for LoginDb and AppDb in Web.config (per explicit request — real DB creds are now committed to this repo).
- Removed the "By department" panel from Dashboard (markup, JS render, and server-side query in GetDashboardExtras); dash-grid is now single-column. Fixed user popover/dropdown-menu items to use flex+gap so options never render edge-to-edge, and widened the gap between the KPI stat strip and the Recent activity panel.
- Logout button now uses a red `.btn-danger` style (was plain outline) on Dashboard/Upload/Repository/Master.
- Master's add-department/category/subcategory/type inputs now get proper font/placeholder/focus styling (were unstyled, just inheriting the browser default since they weren't wrapped in `.field`).
- Upload's file input is now a custom "Choose file" dropzone box (label-triggered, hides the native OS button) instead of the raw browser file picker.
- Replaced native `<select>` popups app-wide with a custom-styled dropdown list (`Scripts/auth.js` `enhanceSelects()` + `.cs-wrap/.cs-trigger/.cs-menu` in app.css) — real `<select>` stays hidden and functional so existing cascade/AJAX JS didn't need to change. Disabled selects also keep a dimmed chevron instead of losing it.
- Applied the Login page's soft radial gradient background app-wide (body), not just the auth screen.
- Fixed Upload save crash: `JSON.parse(res)` was called on an already-parsed object (UploadHandler.ashx returns `application/json`, jQuery auto-parses it) — was throwing `SyntaxError: "[object Object]" is not valid JSON` on every save.
- Repository: added client-side pagination (5 records/page + Prev/Numbered/Next pager), a loading spinner while records fetch, and restructured each record card (subject+date header, category breadcrumb pills, uploader, remark, labeled Tags/Files sections) for clearer UX.
- Upload: added a full-page blocking loader overlay while a record is being saved, so the form can't be touched until the save completes.
- Filtered Data page rebuilt: 4-level dropdown cascade replaced with an expandable filter tree (Department → Category → Sub-Category → Type), files listed inline inside each expanded Type node (lazy-loaded via existing GetFiles), plus search and expand/collapse-all. New tree styles added to Content/app.css.
- Repository: renamed category-level labels (Master's CategoryLevels table) now actually apply to Repository's intro text and search placeholder (`applyRepoLabels()`), matching what Upload already did.
- Repository: fixed files with only 2-3 category levels selected being invisible in the tree — GetFiles now takes a `level` param and matches files that stop exactly at that node, so every level can show its own directly-attached files, not just Type (level 4).
- Repository: Expand all/Collapse all merged into a single toggle button that flips color, icon and label based on current state.
- Repository: hovering a file (220ms delay) shows a popover with its extension chip, Subject and Remark (truncated at 160 chars) and Uploaded date, auto-repositioning near screen edges; file-count under an expanded folder now shows as a "FILES · N" eyebrow label above the rows instead of a floating pill.
- Added offline .msg viewer: vendored MsgReader 3.12.4 + OpenMcdf + Microsoft.IO.RecyclableMemoryStream + RtfPipe (all plain net45 DLLs, no cloud/API calls — parsing happens fully on-server) into packages/csproj/packages.config. New Helpers/MsgParser.cs (offline parse) and Helpers/SecureUpload.cs (shared path-traversal-safe file lookup, factored out of FileHandler's existing checks). New MsgViewer.aspx (+WebMethod GetMsgDetails) renders From/To/Cc/Date/Subject/Body/breadcrumb for a .msg as read-only text (no HTML/script execution). New MsgAttachmentHandler.ashx streams attachments extracted from inside the .msg by index. Repository's file links now route .msg to MsgViewer.aspx instead of downloading it.
- Fixed "Back to Repository" from the .msg viewer bouncing to Login: MsgViewer.aspx opens in a new tab, which doesn't reliably inherit sessionStorage, so it had no token. Repository now appends token+role onto the MsgViewer.aspx link itself, and MsgViewer.aspx resolves/re-persists auth via DTAuth on load (same pattern Login.aspx already uses for Dashboard).
- .msg viewer: fixed real attachments not showing — MsgParser was excluding anything MsgReader flagged `IsInline`, which over-matched real user attachments (we never render the HTML body anyway, so that flag was never meaningful here); now only genuinely `Hidden` items are excluded. Added a working "Download original .msg" link (via the existing FileHandler.ashx) and removed the green "read-only, no scripts executed" info strip per feedback — that was internal dev context, not something the end user needs.
- Upload page: subject autocomplete suggestions are now keyboard-navigable (Up/Down to highlight, Enter to pick, Escape to close) — CSS already had an `.is-active` state ready, JS just never wired it up until now.
- Repository search: typing now also matches by Subject (new `SearchBySubject` WebMethod) and force-expands + reveals the matching file's full department/category/sub-category/type path in the tree, in parallel with the existing name-based folder filter.
- Repository tree: files now list before sub-folders under an expanded node (was folders-first).
- .msg viewer: reordered so Attachments sit between the From/To/Cc/Date block and the body (was after body); To/Cc now render as individual light-pastel pill badges per recipient instead of one long semicolon string; removed the "Back to Repository" link since the viewer always opens in its own tab; footer's "Download original .msg" link stays right-aligned and the filename text now wraps instead of colliding with it on long names.
